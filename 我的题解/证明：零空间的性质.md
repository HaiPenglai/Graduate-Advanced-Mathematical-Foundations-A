### 题目

**问题**：
设 `V` 和 `W` 是两个向量空间，并设 `T₁: V → W` 和 `T₂: V → W` 是两个线性映射。
证明：`null(T₁) ⊆ null(T₂)` 当且仅当 存在一个线性映射 `S: W → W` 使得 `T₂ = S T₁`。

**符号解释**：

*   `null(T₁)`：`T₁` 的零空间，即 `V` 中所有被 `T₁` 映射到 `W` 中零向量的向量集合。
*   `⊆`：子集符号。`A ⊆ B` 意味着集合 `A` 中的每一个元素也都在集合 `B` 中。
*   `T₂ = S T₁`：这是映射的复合。它的意思是对于 `V` 中的任意向量 `v`，都有 `T₂(v) = S(T₁(v))`。即先对 `v` 应用 `T₁`，再对结果应用 `S`。

---

### 题解

这是一个“当且仅当”的证明，所以我们需要证明两个方向：

**第一部分 (⇐): 证明如果存在 `S` 使得 `T₂ = S T₁`，那么 `null(T₁) ⊆ null(T₂)`。**

这个方向比较直接，我们只需要使用定义即可。

**证明**：
1.  **假设**：存在一个线性映射 `S: W → W` 使得 `T₂ = S T₁`。
2.  **目标**：证明 `null(T₁) ⊆ null(T₂)`。为此，我们任取一个向量 `v ∈ null(T₁)`，并证明它也必须属于 `null(T₂)`。
3.  设 `v ∈ null(T₁)`。根据零空间的定义（如你的第二张PPT所示），这意味着 `T₁(v) = 0`。（这里的 `0` 是 `W` 空间中的零向量）。
4.  现在我们来计算 `T₂(v)`：
    $$
    T_2(v) = (S T_1)(v) = S(T_1(v))
    $$
5.  将 `T₁(v) = 0` 代入上式：
    $$
    T_2(v) = S(0)
    $$
6.  因为 `S` 是一个**线性映射**，它必须将零向量映射到零向量。所以 `S(0) = 0`。
7.  因此，`T₂(v) = 0`。
8.  根据零空间的定义，`T₂(v) = 0` 意味着 `v ∈ null(T₂)`。
9.  由于我们从任一 `v ∈ null(T₁)` 出发，都得到了 `v ∈ null(T₂)`，所以我们证明了 `null(T₁) ⊆ null(T₂)`。

**第二部分 (⇒): 证明如果 `null(T₁) ⊆ null(T₂)`，那么存在 `S` 使得 `T₂ = S T₁`。**

这个方向是证明的关键，需要我们**构造**出所需要的线性映射 `S`。

**证明**：
1.  **假设**：`null(T₁) ⊆ null(T₂)`。
2.  **目标**：构造一个线性映射 `S: W → W`，并证明它满足 `T₂ = S T₁`。

3.  **构造 `S` 的思路**：
    `S` 的作用是把 `T₁(v)` 变成 `T₂(v)`。所以，一个很自然的想法是，对于 `W` 空间中任何一个形如 `T₁(v)` 的向量 `w`，我们就定义 `S(w) = T₂(v)`。
    但是，`S` 必须被定义在整个 `W` 空间上，而不仅仅是 `T₁` 的像空间（值域）`Im(T₁)` 上。并且，这个定义必须是**明确的（well-defined）**。

4.  **证明定义的明确性（Well-definedness）**：
    我们需要处理一个问题：如果 `W` 中有一个向量 `w`，它既可以写成 `T₁(v₁)`，又可以写成 `T₁(v₂)`，我们定义的 `S(w)` 到底是 `T₂(v₁)`还是 `T₂(v₂)`？我们必须证明 `T₂(v₁) = T₂(v₂)`，否则我们的定义就是矛盾的。
    *   假设 `w = T₁(v₁) = T₁(v₂)`。
    *   那么 `T₁(v₁) - T₁(v₂) = 0`。由于 `T₁` 是线性的，所以 `T₁(v₁ - v₂) = 0`。
    *   根据零空间的定义，这意味着 `(v₁ - v₂) ∈ null(T₁)`。
    *   现在，使用我们的核心**假设**：`null(T₁) ⊆ null(T₂)`。因此，`(v₁ - v₂) ∈ null(T₂)`。
    *   再次根据零空间的定义，`T₂(v₁ - v₂) = 0`。
    *   由于 `T₂` 是线性的，所以 `T₂(v₁) - T₂(v₂) = 0`，即 `T₂(v₁) = T₂(v₂)`。
    *   **这就证明了我们的定义是明确的**。对于 `Im(T₁)` 中的任何一个向量 `w`，无论它是由哪个 `v` 通过 `T₁` 映射而来，我们定义的 `S(w)` 的结果都是唯一的。

5.  **定义 `S`**：
    *   **第一步**：在子空间 `Im(T₁)` 上定义 `S`。对于任何 `w ∈ Im(T₁)`，存在 `v ∈ V` 使得 `w = T₁(v)`。我们定义 `S(w) = T₂(v)`。
    *   **第二步**：将 `S` 的定义域扩展到整个 `W` 空间。
        取 `Im(T₁)` 的一组基 `{b₁, b₂, ..., bₖ}`。
        将这组基扩展为整个 `W` 空间的一组基 `{b₁, ..., bₖ, c₁, ..., cⱼ}`。
        我们已经定义了 `S` 在 `{bᵢ}` 上的作用。对于新增的基向量 `{cᵢ}`，我们可以任意定义 `S` 的作用。最简单的方式是定义 `S(cᵢ) = 0`。
    *   **第三步**：根据线性映射的基本定理，一个映射在基上的定义可以唯一地线性扩展到整个空间。因此，我们已经成功地在整个 `W` 空间上定义了一个线性映射 `S`。

6.  **验证 `T₂ = S T₁`**：
    我们需要验证对于任意 `v ∈ V`，`T₂(v) = S(T₁(v))` 都成立。
    *   取任意 `v ∈ V`。
    *   令 `w = T₁(v)`。显然 `w ∈ Im(T₁)`。
    *   根据我们在 `Im(T₁)` 上对 `S` 的定义，我们有 `S(w) = T₂(v)`。
    *   将 `w = T₁(v)` 代入，得到 `S(T₁(v)) = T₂(v)`。
    *   这正是 `T₂ = S T₁` 的定义。

**结论**：我们已经证明了两个方向，因此原命题成立。