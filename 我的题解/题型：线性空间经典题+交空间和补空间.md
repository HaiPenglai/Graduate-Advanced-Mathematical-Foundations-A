### 题型：线性表示

![image-20251213203754219](./assets/image-20251213203754219.png)

### 题型：线性相关性

![image-20251213203839955](./assets/image-20251213203839955.png)

![image-20251213203903774](./assets/image-20251213203903774.png)

#### 问题复述

在四维空间 `R⁴` 中，给定以下四个向量：

$$
\alpha_1 = \begin{pmatrix} 0 \\ 2 \\ 1 \\ 0 \end{pmatrix}, \quad \alpha_2 = \begin{pmatrix} 1 \\ -1 \\ 0 \\ 0 \end{pmatrix}, \quad \alpha_3 = \begin{pmatrix} 1 \\ 2 \\ 0 \\ -1 \end{pmatrix}, \quad \alpha_4 = \begin{pmatrix} 1 \\ 0 \\ 0 \\ 1 \end{pmatrix}
$$

请求讨论这组向量的线性相关性。

#### 题解

为了讨论向量组 `α₁, α₂, α₃, α₄` 的线性相关性，我们可以将这些向量作为列向量构成一个矩阵 `A`，然后通过求该矩阵的秩来判断。

**方法：**
如果矩阵 `A` 的秩 `r(A)` 小于向量的个数（即 `r(A) < 4`），则向量组线性相关。
如果矩阵 `A` 的秩 `r(A)` 等于向量的个数（即 `r(A) = 4`），则向量组线性无关。

**步骤如下：**

1.  **构建矩阵 A**
    将向量 `α₁, α₂, α₃, α₄` 作为列向量，构成矩阵 `A`：
    $$
    A = (\alpha_1, \alpha_2, \alpha_3, \alpha_4) = \begin{pmatrix} 0 & 1 & 1 & 1 \\ 2 & -1 & 2 & 0 \\ 1 & 0 & 0 & 0 \\ 0 & 0 & -1 & 1 \end{pmatrix}
    $$

2.  **进行初等行变换**
    我们的目标是将矩阵 `A` 化为阶梯形矩阵，以确定其秩。

    将第1行与第3行互换 (`r₁ ↔ r₃`)：
    $$
    \begin{pmatrix} 1 & 0 & 0 & 0 \\ 2 & -1 & 2 & 0 \\ 0 & 1 & 1 & 1 \\ 0 & 0 & -1 & 1 \end{pmatrix}
    $$

    将第2行减去2倍的第1行 (`r₂ - 2r₁`)：
    $$
    \begin{pmatrix} 1 & 0 & 0 & 0 \\ 0 & -1 & 2 & 0 \\ 0 & 1 & 1 & 1 \\ 0 & 0 & -1 & 1 \end{pmatrix}
    $$

    将第3行加上第2行 (`r₃ + r₂`)：
    $$
    \begin{pmatrix} 1 & 0 & 0 & 0 \\ 0 & -1 & 2 & 0 \\ 0 & 0 & 3 & 1 \\ 0 & 0 & -1 & 1 \end{pmatrix}
    $$

    将第4行乘以3，再加上第3行 (`3r₄ + r₃`)：
    $$
    \begin{pmatrix} 1 & 0 & 0 & 0 \\ 0 & -1 & 2 & 0 \\ 0 & 0 & 3 & 1 \\ 0 & 0 & 0 & 4 \end{pmatrix}
    $$

3.  **确定秩并得出结论**
    经过初等行变换，我们得到了一个阶梯形矩阵。该矩阵有4个非零行。因此，矩阵 `A` 的秩为4。
    $$
    r(A) = 4
    $$

    由于矩阵的秩等于向量的个数（`r(A) = 4`），所以该向量组线性无关。

**结论：** 向量组 `α₁, α₂, α₃, α₄` 是线性无关的。

###  题型：线性空间的基

![image-20251213203927710](./assets/image-20251213203927710.png)

![image-20251213203939044](./assets/image-20251213203939044.png)

#### 问题复述

**例3** 证明：在三维向量空间 `R³` 中，向量组 `x₁, x₂, x₃` 与向量组 `y₁, y₂, y₃` 都是线性空间 `R³` 的一组基。

其中，向量组 `x` 为：
$$
x_1 = \begin{pmatrix} 1 \\ 0 \\ 0 \end{pmatrix}, \quad x_2 = \begin{pmatrix} 0 \\ 1 \\ 0 \end{pmatrix}, \quad x_3 = \begin{pmatrix} 0 \\ 0 \\ 1 \end{pmatrix}
$$

向量组 `y` 为：
$$
y_1 = \begin{pmatrix} 1 \\ 0 \\ 0 \end{pmatrix}, \quad y_2 = \begin{pmatrix} 1 \\ 1 \\ 0 \end{pmatrix}, \quad y_3 = \begin{pmatrix} 1 \\ 1 \\ 1 \end{pmatrix}
$$

#### 题解

根据线性空间基的定义，要证明一个向量组是 `n` 维空间 `Rⁿ` 的基，需要证明这个向量组由 `n` 个线性无关的向量构成。本题是在 `R³` 空间中，因此我们需要证明这两个向量组分别由3个线性无关的向量构成。

判断 `n` 个 `n` 维向量是否线性无关，可以把它们构成一个 `n x n` 的矩阵，如果该矩阵的秩等于 `n` (即满秩)，则向量组线性无关。我们将使用初等行变换求秩的方法来证明。

1. **证明向量组 `x₁, x₂, x₃` 是 `R³` 的一组基**

首先，将向量 `x₁, x₂, x₃` 作为列向量构成矩阵 `A`：
$$
A = (x_1, x_2, x_3) = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{pmatrix}
$$
这个矩阵是三阶单位矩阵，其本身就是一个最简阶梯形矩阵。矩阵中非零行的数量为3，因此矩阵 `A` 的秩为3。
$$
r(A) = 3
$$
由于矩阵 `A` 的秩等于向量的个数 (3)，也等于空间的维数 (3)，所以向量组 `x₁, x₂, x₃` 线性无关。

因此，向量组 `{x₁, x₂, x₃}` 是 `R³` 的一组基。

**2. 证明向量组 `y₁, y₂, y₃` 是 `R³` 的一组基**

接着，将向量 `y₁, y₂, y₃` 作为列向量构成矩阵 `B`：
$$
B = (y_1, y_2, y_3) = \begin{pmatrix} 1 & 1 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 1 \end{pmatrix}
$$
这个矩阵已经是一个上三角矩阵，也是一个阶梯形矩阵。我们可以清楚地看到，矩阵中非零行的数量为3。因此，矩阵 `B` 的秩为3。
$$
r(B) = 3
$$
由于矩阵 `B` 的秩等于向量的个数 (3)，也等于空间的维数 (3)，所以向量组 `y₁, y₂, y₃` 线性无关。

因此，向量组 `{y₁, y₂, y₃}` 也是 `R³` 的一组基。

**结论：**
通过初等行变换求秩的方法，我们证明了矩阵 `A` 和矩阵 `B` 都是满秩的，从而说明了向量组 `{x₁, x₂, x₃}` 和 `{y₁, y₂, y₃}` 都是线性无关的。因此，它们都是三维向量空间 `R³` 的基。这也验证了线性空间的基不唯一。

好的，我们来详细解答这道关于子空间运算的题目，并按照要求使用初等行变换的方法求解。

### 交空间、和空间

![image-20251213204758621](./assets/image-20251213204758621.png)

![image-20251213204807242](./assets/image-20251213204807242.png)

#### 问题复述

**例4** 已知四维空间 `R⁴` 中的两个子空间：
`L₁` 是由向量 `α₁, α₂` 生成的子空间，记为 `L₁ = L(α₁, α₂)`.
`L₂` 是由向量 `β₁, β₂` 生成的 " " " " ，记为 `L₂ = L(β₁, β₂)`.

其中向量为：
$$
\alpha_1 = \begin{pmatrix} 1 \\ 2 \\ 1 \\ 0 \end{pmatrix}, \quad \alpha_2 = \begin{pmatrix} -1 \\ 1 \\ 1 \\ 1 \end{pmatrix}, \quad \beta_1 = \begin{pmatrix} 2 \\ -1 \\ 0 \\ 1 \end{pmatrix}, \quad \beta_2 = \begin{pmatrix} 1 \\ -1 \\ 3 \\ 7 \end{pmatrix}
$$

请求出它们的**和空间** `L₁ + L₂` 与**交空间** `L₁ ∩ L₂` 的维数与一组基。

#### 题解

#### 1. 求解和空间 `L₁ + L₂` 的维数与基

和空间 `L₁ + L₂`是由 `L₁` 和 `L₂` 的所有生成向量 `α₁, α₂, β₁, β₂` 共同生成的线性空间。即：
$$
L_1 + L_2 = L(\alpha_1, \alpha_2, \beta_1, \beta_2)
$$
为了求其维数和基，我们需要从这四个向量中找出一个极大线性无关组。我们采用初等行变换求秩的方法。

**步骤：**
将向量 `α₁, α₂, β₁, β₂` 作为**列向量**构成矩阵A，然后对其进行初等行变换，化为阶梯形矩阵。

$$
A = (\alpha_1, \alpha_2, \beta_1, \beta_2) = \begin{pmatrix} 1 & -1 & 2 & 1 \\ 2 & 1 & -1 & -1 \\ 1 & 1 & 0 & 3 \\ 0 & 1 & 1 & 7 \end{pmatrix}
$$

进行初等行变换：
$$
\begin{pmatrix} 1 & -1 & 2 & 1 \\ 2 & 1 & -1 & -1 \\ 1 & 1 & 0 & 3 \\ 0 & 1 & 1 & 7 \end{pmatrix} \xrightarrow[r_3-r_1]{r_2-2r_1} \begin{pmatrix} 1 & -1 & 2 & 1 \\ 0 & 3 & -5 & -3 \\ 0 & 2 & -2 & 2 \\ 0 & 1 & 1 & 7 \end{pmatrix} \xrightarrow{r_2 \leftrightarrow r_4} \begin{pmatrix} 1 & -1 & 2 & 1 \\ 0 & 1 & 1 & 7 \\ 0 & 2 & -2 & 2 \\ 0 & 3 & -5 & -3 \end{pmatrix}
$$
$$
\xrightarrow[r_4-3r_2]{r_3-2r_2} \begin{pmatrix} 1 & -1 & 2 & 1 \\ 0 & 1 & 1 & 7 \\ 0 & 0 & -4 & -12 \\ 0 & 0 & -8 & -24 \end{pmatrix} \xrightarrow[r_4-2r_3]{r_3 \div (-4)} \begin{pmatrix} 1 & -1 & 2 & 1 \\ 0 & 1 & 1 & 7 \\ 0 & 0 & 1 & 3 \\ 0 & 0 & 0 & 0 \end{pmatrix}
$$
矩阵 `A` 的秩 `r(A) = 3`，因为阶梯形矩阵有3个非零行。
这说明向量组 `α₁, α₂, β₁, β₂` 的极大线性无关组包含3个向量。阶梯形矩阵的主元（首个非零元）分别位于第1、2、3列，因此原矩阵的前三个向量 `α₁, α₂, β₁` 构成一个极大线性无关组。

**结论 (和空间):**
*   和空间 `L₁ + L₂` 的维数是 `dim(L₁ + L₂) = r(A) = 3`。
*   和空间 `L₁ + L₂` 的一组基是 `{α₁, α₂, β₁}`。

#### 2. 求解交空间 `L₁ ∩ L₂` 的维数与基

首先，利用维数公式计算交空间的维数：
$$
\dim(L_1 \cap L_2) = \dim(L_1) + \dim(L_2) - \dim(L_1 + L_2)
$$
`L₁` 由两个线性无关的向量 `α₁, α₂` 生成，`L₂` 由两个线性无关的向量 `β₁, β₂` 生成，所以 `dim(L₁) = 2`，`dim(L₂) = 2`。
$$
\dim(L_1 \cap L_2) = 2 + 2 - 3 = 1
$$
交空间的维数是1，因此其基只包含一个非零向量。

**步骤：**
设任意一个向量 `α ∈ L₁ ∩ L₂`。
因为 `α ∈ L₁`，所以 `α` 可以表示为 `α = k₁α₁ + k₂α₂`。
因为 `α ∈ L₂`，所以 `α` 可以表示为 `α = l₁β₁ + l₂β₂`。
因此，我们有：
$$
k_1\alpha_1 + k_2\alpha_2 = l_1\beta_1 + l_2\beta_2
$$
$$
k_1\alpha_1 + k_2\alpha_2 - l_1\beta_1 - l_2\beta_2 = 0
$$
这是一个关于 `k₁, k₂, l₁, l₂` 的齐次线性方程组。将向量坐标代入，得到：
$$
\begin{cases} k_1 - k_2 - 2l_1 - l_2 = 0 \\ 2k_1 + k_2 + l_1 + l_2 = 0 \\ k_1 + k_2 - 3l_2 = 0 \\ k_2 - l_1 - 7l_2 = 0 \end{cases}
$$
其系数矩阵正是我们在第一步中进行行变换的矩阵 `A` 的转置（或者说，如果我们对行向量组成的矩阵进行变换，会得到相同的结果）。我们可以直接解这个方程组（解法与对矩阵A的行变换过程一致）。将系数矩阵化为阶梯形：
$$
\begin{pmatrix} 1 & -1 & -2 & -1 \\ 2 & 1 & 1 & 1 \\ 1 & 1 & 0 & -3 \\ 0 & 1 & -1 & -7 \end{pmatrix} \rightarrow \begin{pmatrix} 1 & -1 & -2 & -1 \\ 0 & 1 & -1 & -7 \\ 0 & 0 & 1 & 3 \\ 0 & 0 & 0 & 0 \end{pmatrix}
$$
由此得到方程组：
$$
\begin{cases} k_1 - k_2 - 2l_1 - l_2 = 0 \\ k_2 - l_1 - 7l_2 = 0 \\ l_1 + 3l_2 = 0 \end{cases}
$$
取自由变量 `l₂ = -1` (为方便计算，也可以取1)，则：
*   `l₁ = -3l₂ = 3`
*   `k₂ = l₁ + 7l₂ = 3 + 7(-1) = -4`
*   `k₁ = k₂ + 2l₁ + l₂ = -4 + 2(3) + (-1) = 1`

我们得到了方程组的一组非零解 `(k₁, k₂, l₁, l₂) = (1, -4, 3, -1)`。
将 `k₁`, `k₂` 的值代入 `α = k₁α₁ + k₂α₂`：
$$
\alpha = 1 \cdot \alpha_1 - 4 \cdot \alpha_2 = 1 \begin{pmatrix} 1 \\ 2 \\ 1 \\ 0 \end{pmatrix} - 4 \begin{pmatrix} -1 \\ 1 \\ 1 \\ 1 \end{pmatrix} = \begin{pmatrix} 1+4 \\ 2-4 \\ 1-4 \\ 0-4 \end{pmatrix} = \begin{pmatrix} 5 \\ -2 \\ -3 \\ -4 \end{pmatrix}
$$
这个向量 `α` 就是交空间 `L₁ ∩ L₂` 的一个基。

**结论 (交空间):**

*   交空间 `L₁ ∩ L₂` 的维数是 `1`。
*   交空间 `L₁ ∩ L₂` 的一组基是$ \left\{ \begin{pmatrix} 5 \\ -2 \\ -3 \\ -4 \end{pmatrix} \right\}$。